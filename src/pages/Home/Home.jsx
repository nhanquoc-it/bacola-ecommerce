import React from "react";
import { HomeBanner } from "~/components/HomeBanner";

const Home = () => {
	return (
		<section className="home-section">
			<HomeBanner />
		</section>
	);
};

export default Home;
